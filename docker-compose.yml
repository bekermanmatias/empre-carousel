services:
  renderer:
    build: .
    restart: unless-stopped
    environment:
      PORT: "3001"
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL}
    ports:
      - "127.0.0.1:${RENDER_PORT:-3001}:3001"
    volumes:
      - renderer_jobs:/app/output/jobs

volumes:
  renderer_jobs:
