import { useEffect, useRef, useState } from 'react'
export function OverflowText({ children, className = '', field }: { children?: string; className?: string; field?: string }) { const ref = useRef<HTMLDivElement>(null); const [overflow, setOverflow] = useState(false); useEffect(() => { const node = ref.current; if (!node) return; const value = node.scrollHeight > node.clientHeight + 1 || node.scrollWidth > node.clientWidth + 1; setOverflow(value); if (value) console.warn(`Empre Carousel: overflow detected in ${field || className || 'text box'}`) }, [children, className, field]); return <div ref={ref} data-overflow-field={field} className={`${className} ${overflow ? 'has-overflow' : ''}`}>{children}</div> }
